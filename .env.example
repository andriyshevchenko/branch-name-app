# OpenRouter API Key (Server-side only)
# 
# IMPORTANT: This API key is NEVER exposed to the frontend.
# It's only used on the server-side via /api/generate-branch-name endpoint.
#
# For Vercel Deployment:
# Add VITE_OPENROUTER_API_KEY as an environment variable in Vercel project settings
#
# For local development:
# Copy this file to .env.local and add your key:
# VITE_OPENROUTER_API_KEY=your_api_key_here
#
# Get your free API key at: https://openrouter.ai/settings/keys

#
# For Vercel deployment, add this as an environment variable in your project settings
VITE_OPENROUTER_API_KEY=
